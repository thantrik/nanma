import React from "react";
import { FontIcon, mergeStyles } from "@fluentui/react";

const icon = (
  <FontIcon
    iconName="camera"
    className={mergeStyles({
      fontSize: 12,
      marginRight: 0,
    })}
  />
);

export default icon;
